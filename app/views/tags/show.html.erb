<h1><%= @tag.name %></h1>
<% @artworks_current_user.each do |artwork| %>
  <%= artwork.name %>
<% end %>
<% @artworks_current_user.tagged_with([@tag.name], :any => true).each do |artwork| %>
  <p><%= artwork.name %> : <%= artwork.tag_list %></p>
<% end %>



