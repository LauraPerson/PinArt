<div class="" data-search-tags-target="list">
  <% @events.each do |event| %>
    <p><%= link_to event.name, event_path(event) %></p>
  <% end %>
  <% @owned_tags.each do |tag| %>
    <p><%= link_to tag.name, tag_path(tag) %></p>


  <% end %>
</div>